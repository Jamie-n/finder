<a href="{{$link ?? ''}}" class="col-4 text-decoration-none">
    <div class=" car-card transition text-center p-3">
        <img class="mb-3" src="{{$imgSrc}}" alt="" style="max-width: 250px; height: 80px">
        <p class="fw-bold text-light">{{$title}}</p>
    </div>
</a>
